<h1>{{ sections.settings.title }}</h1>
<p>{{ sections.settings.subtext_title | t }}</p>



{% schema %}
  {
    "name": "Template 404",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "default": "404",
        "label": "404 Heading Title"
      }, {
        "type": "text",
        "id": "subtext_title",
        "default": "The page you are looking for does not found",
        "label": "Oops, the page you are looking for is not found"
      }
    ]
  }
{% endschema %}